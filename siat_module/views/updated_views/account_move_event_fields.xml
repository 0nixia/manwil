<odoo>
    <data>
        <!-- Form view extension for account.move -->
        <record id="view_move_form_extension" model="ir.ui.view">
            <field name="name">account.move.form.extension</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//group[@id='header_right_group']" position="inside">
                    <field name="has_event" invisible="1"/>
                    <field name="siat_num_invoice" invisible="has_event not in [5, 6, 7]" readonly="state != 'draft'"/>
                    <field name="siat_date_invoice" invisible="has_event not in [5, 6, 7]" readonly="state != 'draft'"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
